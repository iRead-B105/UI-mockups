# 테스트 세션 변경사항

- 작성일: 2026-07-23
- 대상 프로젝트: `apps/learner`
- 기준 문서: `테스트_구현_명세.md`
- 변경 성격: 학습자 테스트 진입 연결, 문항 전환 분리, 목업 입력 통과 지원

## 1. 변경 요약

학습자 메인 화면의 `실력 도전`에서 테스트 안내 화면으로 이동할 수 있도록 진입 경로를 연결했다.

테스트 문항을 제출하면 자동으로 다음 문항을 표시하던 구조를 다음 두 단계로 분리했다.

1. 현재 문항의 최종 응답을 기록하고 다음 문항을 계산한다.
2. 중립 피드백에서 학습자가 `다음으로`를 누르면 계산된 다음 문항을 표시한다.

마이크, 시선 추적기, 필기 판정기 등 실제 입력 기능이 연결되지 않은 현재 목업에서도 전체 테스트 흐름을 확인할 수 있도록 목업용 `다음으로` 기능을 추가했다. 목업으로 통과한 문항은 오답이 아니라 평가 제외 응답으로 저장한다.

## 2. 변경 원칙

`테스트_구현_명세.md`의 테스트와 훈련 분리 원칙을 유지한다.

| 구분 | 테스트 세션 적용 내용 |
|---|---|
| 최종 응답 | 평가 가능한 응답은 문항당 한 번만 기록 |
| 제출 전 수정 | 선택, 배열, 필기 등의 수정 허용 |
| 제출 후 재시도 | 기술 오류 외에는 허용하지 않음 |
| 힌트 | 제공하지 않음 |
| 정답 공개 | 제공하지 않음 |
| 정오 피드백 | 제공하지 않음 |
| 완료 피드백 | 정오와 무관한 중립 피드백 사용 |
| 다음 문항 | 테스트 전용 적응형 엔진이 결정 |
| 목업 통과 | 평가 제외로 기록하고 다음 문항으로 이동 |

훈련의 `다음으로` 화면 패턴은 참고하지만 다음 훈련 기능은 테스트 세션에서 사용하지 않는다.

- 훈련 세션 상태
- 정오 피드백
- 단계별 힌트
- 정답 강조
- 학습 목적의 반복 재시도
- 훈련 완료 판정

테스트 상태와 문항 판정은 계속 `features/assessment` 영역에서 독립적으로 관리한다.

## 3. 테스트 진입과 라우트

### 3.1 메인 화면 진입

기존에는 메인 섬에서 `실력 도전`을 선택해도 안내 토스트만 표시되고 화면이 이동하지 않았다.

변경 후에는 다음 흐름으로 이동한다.

```text
학습자 메인
→ 실력 도전 선택
→ 테스트 안내
→ 테스트 시작 또는 이어하기
→ 테스트 진행
→ 테스트 완료
→ 학습자 홈
```

### 3.2 등록 라우트

| 경로 | 라우트 이름 | 역할 |
|---|---|---|
| `/learner/assessment` | `assessment-intro` | 테스트 안내, 기기 상태 확인, 시작·이어하기 |
| `/learner/assessment/session` | `assessment-session` | 테스트 문항 진행 |
| `/learner/assessment/complete` | `assessment-complete` | 테스트 완료 안내 |

### 3.3 라우트 보호

- 저장된 테스트 세션 없이 진행 경로에 접근하면 테스트 안내 화면으로 이동한다.
- 완료된 세션으로 진행 경로에 접근하면 완료 화면으로 이동한다.
- 완료되지 않은 세션으로 완료 경로에 접근하면 테스트 안내 화면으로 이동한다.
- 테스트 진행 화면과 완료 화면에서는 일반 학습자 헤더를 숨긴다.
- 테스트 진행 화면에서는 테스트 전용 진행률과 나가기 버튼을 사용한다.

## 4. 일반 응답의 다음 문항 전환

### 4.1 변경 전

기존 `submit()`은 다음 작업을 한 번에 처리했다.

```text
응답 저장
→ 정오 판정
→ 적응형 다음 문항 계산
→ currentItemId 즉시 변경
→ 일정 시간 동안 중립 피드백 표시
```

중립 피드백이 표시되는 시점에 내부 현재 문항은 이미 다음 문항으로 변경되어 있었다.

### 4.2 변경 후

응답 확정과 화면 이동을 분리했다.

```text
응답 입력
→ 답 보내기
→ 최종 응답 저장
→ 정오 판정
→ 적응형 다음 문항 계산
→ 다음 문항 대기 상태 저장
→ 중립 피드백 표시
→ 학습자가 다음으로 선택
→ 다음 문항 표시
```

응답을 제출한 직후에는 현재 문항 ID를 유지한다. 적응형 엔진이 결정한 다음 문항 ID만 대기 상태로 보관한다.

중립 피드백에는 다음 정보만 표시한다.

- `선택했어요`
- `답을 기록했어요`
- `다음으로`

정답, 오답, 점수, 정답 카드, 캐릭터 반응은 표시하지 않는다.

## 5. 테스트 세션 상태 변경

`AssessmentSession`에 다음 상태를 추가했다.

| 상태 | 형식 | 역할 |
|---|---|---|
| `awaitingNext` | `boolean` | 응답 기록을 마치고 다음 문항 이동을 기다리는지 표시 |
| `pendingNextItemId` | `string \| null` | 적응형 엔진이 결정한 다음 문항 ID |
| `pendingComplete` | `boolean` | 다음 이동이 새 문항이 아니라 테스트 완료인지 표시 |

### 5.1 제출 처리

`submit()`은 다음 순서로 동작한다.

1. 세션이 진행 중인지 확인한다.
2. 다른 전환이 진행 중인지 확인한다.
3. 이미 다음 문항 대기 상태인지 확인한다.
4. 응답을 판정하고 응답 이벤트를 저장한다.
5. 현재 문항을 시행 문항 목록에 추가한다.
6. 적응형 엔진으로 다음 문항을 계산한다.
7. 다음 문항 ID와 완료 여부를 대기 상태로 저장한다.
8. 중립 피드백을 표시한다.

대기 상태에서는 같은 문항을 다시 제출할 수 없다.

### 5.2 다음 문항 처리

새로 추가한 `nextItem()`은 다음 순서로 동작한다.

1. 다음 문항 대기 상태인지 확인한다.
2. 전환 잠금을 설정한다.
3. 중립 피드백을 닫는다.
4. `pendingComplete=true`이거나 다음 문항이 없으면 테스트를 완료한다.
5. 다음 문항이 있으면 `currentItemId`를 변경한다.
6. 대기 상태를 초기화하고 세션을 저장한다.
7. 다음 이벤트 루프에서 전환 잠금을 해제한다.

전환 잠금을 다음 이벤트 루프까지 유지하므로 빠르게 연속 클릭해도 다음 문항을 두 개 이상 건너뛰지 않는다.

## 6. 목업용 다음 문항 이동

### 6.1 목적

현재 목업에는 다음 입력 또는 평가 기능이 완전히 연결되어 있지 않다.

- 마이크 및 음성 인식
- 실제 시선 추적기
- 실제 필기 판정기
- 장치별 입력 품질 판정

입력 기능 때문에 테스트 흐름이 중단되지 않도록 테스트 문항 화면에 목업용 `다음으로` 영역을 추가했다.

화면에는 다음 안내를 표시한다.

```text
입력 기능을 연결하기 전 사용하는 목업 이동입니다.
[다음으로]
```

목업 기능은 `assessmentPolicy.mockNavigationEnabled`로 제어하며 현재 값은 `true`다.

### 6.2 목업 통과 처리

목업용 `다음으로`를 누르면 `skipCurrentItemForMock()`이 실행된다.

```text
목업용 다음으로
→ 현재 문항을 평가 제외 응답으로 저장
→ 적응형 다음 문항 계산
→ nextItem() 실행
→ 다음 문항 표시 또는 테스트 완료
```

목업 통과는 별도의 정답이나 임의 오답을 생성하지 않는다.

### 6.3 저장 값

| 항목 | 저장 값 |
|---|---|
| `responseState` | `technical_invalid` |
| `responsePayload` | `null` |
| `isEvaluable` | `false` |
| `isCorrect` | `null` |
| `observedErrorCodes` | 빈 배열 |
| `technicalError.code` | `MOCK_INPUT_UNAVAILABLE` |
| `technicalError.message` | 목업 입력 없이 이동했다는 설명 |

목업 통과 문항은 시행 문항 목록에는 포함한다. 따라서 같은 문항이 다시 선택되지 않는다.

다음 계산에서는 제외한다.

- 영역별 정답률
- 독립 정답 횟수
- 연속 정답 횟수
- 반복 관찰 오류
- 난이도 상승 또는 하락 근거
- 추천 훈련의 정오 근거

모든 문항을 목업으로 통과해도 사용 가능한 다음 문항이 없으면 테스트 완료 상태에 도달한다.

## 7. 저장과 재개

다음 문항 대기 상태는 세션과 함께 `localStorage`에 저장한다.

따라서 응답을 제출한 후 `다음으로`를 누르기 전에 중단해도 다음 상태를 복원할 수 있다.

- 현재 제출 문항
- 저장된 최종 응답
- 계산된 다음 문항
- 테스트 완료 예정 여부
- 중립 피드백 표시 여부

이전 버전에서 저장한 세션에는 신규 상태가 없을 수 있으므로 세션을 불러올 때 다음 기본값을 보완한다.

```text
awaitingNext = false
pendingNextItemId = null
pendingComplete = false
```

제출을 마친 현재 문항은 시행 문항 목록에 포함되어 있으므로 재개 시 미제출 문항처럼 폐기하거나 중복 저장하지 않는다.

## 8. 화면 변경

### 8.1 테스트 안내

- 기존 테스트 설명을 유지한다.
- 신규 세션이면 `시작하기`를 표시한다.
- 중단된 세션이면 `이어서 하기`를 표시한다.
- 안내 화면은 일반 학습자 헤더를 유지하여 홈으로 돌아갈 수 있게 한다.

### 8.2 테스트 진행

- 일반 학습자 헤더를 숨긴다.
- 테스트 전용 홈 버튼과 진행률을 표시한다.
- 기존 테스트 활동 컴포넌트를 유지한다.
- 목업용 이동 안내와 `다음으로`를 추가한다.
- 정상 응답 제출 후에는 중립 피드백의 `다음으로`를 표시한다.

### 8.3 테스트 완료

- 점수, 정답 수, 오답 문항을 표시하지 않는다.
- 완료 사실과 교수자 확인 안내만 표시한다.
- 홈 이동 버튼을 제공한다.

## 9. 변경 파일

| 파일 | 변경 내용 |
|---|---|
| `apps/learner/src/views/learner/LearnerHomeView.vue` | `실력 도전`을 테스트 안내 라우트와 연결 |
| `apps/learner/src/app/router/index.ts` | 테스트 안내·진행·완료 라우트와 접근 보호 추가 |
| `apps/learner/src/views/learner/AssessmentSessionView.vue` | 테스트 전용 다음 문항 처리와 목업 이동 UI 연결 |
| `apps/learner/src/features/assessment/components/NeutralFeedback.vue` | 중립 피드백에 수동 `다음으로` 버튼 추가 |
| `apps/learner/src/features/assessment/composables/useAssessmentSession.ts` | 응답 확정과 문항 이동 분리, 목업 통과, 이전 세션 정규화 |
| `apps/learner/src/features/assessment/types/assessment-session.ts` | 다음 문항 대기 상태와 목업 기술 오류 코드 추가 |
| `apps/learner/src/features/assessment/types/assessment-policy.ts` | 목업 이동 정책 타입 추가 |
| `apps/learner/src/features/assessment/config/assessment-policy.ts` | 목업 이동 활성화 설정 추가 |
| `apps/learner/src/features/assessment/__tests__/assessment-session.spec.ts` | 일반 이동, 목업 통과, 완료 및 연속 클릭 검증 추가 |

## 10. 수정하지 않은 주요 영역

| 파일 또는 영역 | 유지 내용 |
|---|---|
| `apps/learner/src/features/training/**` | 훈련 재시도, 힌트, 피드백, 완료 상태 |
| `apps/learner/src/composables/useTrainingSession.ts` | 기존 훈련 세션 |
| `apps/learner/src/features/assessment/engine/scoring-engine.ts` | 일반 테스트 응답 판정 |
| `apps/learner/src/features/assessment/engine/adaptive-engine.ts` | 적응형 문항 선택 규칙 |
| `apps/learner/src/features/assessment/engine/recommendation-engine.ts` | 결과 및 추천 계산 |
| 교수자 프로젝트 | 결과 조회와 커리큘럼 연결 |
| 백엔드·API | 실제 세션 생성과 서버 저장 |

## 11. 검증 결과

### 11.1 타입 및 빌드

- TypeScript 및 Vue 타입 검사: 통과
- Vite 프로덕션 빌드: 통과

### 11.2 자동 테스트

- 테스트 파일: 5개 통과
- 테스트 항목: 총 16개 통과

이번 변경에서 추가로 확인한 내용은 다음과 같다.

1. 응답 제출 후 현재 문항이 유지되는가
2. `다음으로`를 누른 뒤 다음 문항으로 변경되는가
3. 목업 통과 응답이 평가 제외로 저장되는가
4. 목업 통과가 관찰 오류를 만들지 않는가
5. 빠른 연속 목업 이동이 차단되는가
6. 목업 이동만으로도 테스트 완료 상태에 도달하는가

### 11.3 화면 검증 제한

로컬 주소에 대한 앱 브라우저 보안 정책으로 브라우저 자동 클릭 검증은 수행하지 못했다. 타입 검사, 프로덕션 빌드 및 테스트 세션 자동 테스트로 코드 동작을 검증했으며, 실제 배포 또는 로컬 브라우저에서 다음 수동 확인이 필요하다.

1. 메인 섬의 `실력 도전` 클릭
2. 안내 화면의 `시작하기`
3. 정상 제출 후 중립 피드백의 `다음으로`
4. 입력 없는 목업용 `다음으로`
5. 마지막 문항 이후 완료 화면 이동
6. 홈 이동 후 중단 세션 이어하기

## 12. 후속 개발 시 주의사항

1. 실제 입력 기능이 연결되면 `mockNavigationEnabled`를 `false`로 변경하거나 운영 환경 정책에서 비활성화한다.
2. 목업 통과 응답을 오답으로 마이그레이션하지 않는다.
3. 목업 통과 응답은 실제 학습 추천 근거로 사용하지 않는다.
4. 실제 서버 세션을 도입할 때 `awaitingNext`, `pendingNextItemId`, `pendingComplete`를 체크포인트 계약에 포함한다.
5. 실제 입력 장치 오류와 목업 통과 사유를 동일한 운영 통계로 집계하지 않는다.
6. 테스트의 `nextItem()`을 훈련 세션의 `nextItem()`과 합치지 않는다.
7. 중립 피드백에는 정답 여부를 추론할 수 있는 색상, 문구, 소리 또는 캐릭터 반응을 추가하지 않는다.
8. 진행률 정책을 변경할 때 평가 가능 문항 수와 시행 문항 수를 구분한다.
