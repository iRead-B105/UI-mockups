# 훈련 세션 변경사항

- 작성일: 2026-07-23
- 대상 프로젝트: `apps/learner`
- 기준 문서: `훈련 상세 내용.md`
- 변경 성격: 학습자 훈련 선택 구조와 세션 진입 경로 재구성

## 1. 변경 요약

학습자 프론트의 훈련 선택 구조를 `훈련 상세 내용.md`에 맞춰 다음 3개 대분류로 재구성했다.

1. 음운 인식 및 파닉스
2. 글 해독 및 문장 이해
3. 유창성

화면에 표시되는 훈련은 총 28개이며, 중복되는 훈련과 한글 대결은 노출하지 않는다.

이번 작업에서는 훈련 세션의 평가, 시도 횟수, 녹음, 결과 저장 방식은 변경하지 않았다. 화면에서 훈련을 분류하고 세션에 진입하는 과정만 변경했으며, 세션 내부에서는 기존 훈련 ID와 기존 카테고리 ID를 계속 사용한다.

## 2. 변경 전후 구조

### 2.1 변경 전

훈련 선택 화면은 다음 4개 기술 카테고리를 직접 노출했다.

| 기존 카테고리 ID | 기존 화면 분류 |
|---|---|
| `phonological-awareness` | 음운 인식 훈련 |
| `phonics` | 파닉스 훈련 |
| `short-text` | 짧은 글 훈련 |
| `fluency` | 유창성 훈련 |

기술 카테고리가 그대로 화면 대분류로 사용되어 문서상의 훈련 체계와 화면 구성이 일치하지 않았다.

### 2.2 변경 후

화면에는 다음 3개 표시 전용 대분류를 사용한다.

| 표시 카테고리 ID | 화면 대분류 | 세부 훈련 |
|---|---|---|
| `sound-phonics` | 음운 인식 및 파닉스 | 글자 따라 보기, 소리 듣고 고르기, 글자 만들기, 글자 자르기, 글자 대치 |
| `decoding-comprehension` | 글 해독 및 문장 이해 | 글 해독, 문장 완성 및 이해 |
| `fluency` | 유창성 | 유창하게 읽기 |

표시 카테고리 ID는 훈련 선택 화면과 세부 훈련 모달을 여는 용도로만 사용한다.

## 3. 최종 훈련 구성과 기존 세션 ID 연결

### 3.1 음운 인식 및 파닉스

| 세부 훈련 | 화면 표시명 | 기존 훈련 ID | 세션 카테고리 ID |
|---|---|---|---|
| 글자 따라 보기 | 모음 | `trace-vowel` | `phonics` |
| 글자 따라 보기 | 자음 | `trace-consonant` | `phonics` |
| 글자 따라 보기 | 음절 | `trace-syllable` | `phonics` |
| 소리 듣고 고르기 | 자음과 모음 | `letter-sound-choice` | `phonics` |
| 소리 듣고 고르기 | 첫소리 찾기 | `first-sound` | `phonological-awareness` |
| 소리 듣고 고르기 | 같은 첫소리의 낱말 찾기 | `same-sound` | `phonological-awareness` |
| 소리 듣고 고르기 | 받침 소리(종성), 끝소리 | `last-sound` | `phonological-awareness` |
| 소리 듣고 고르기 | 비슷한 소리('ㄱ', 'ㅋ' 등) | `similar-sound` | `phonics` |
| 글자 만들기 | 소리 합치기(음절·낱말 합성) | `sound-combine` | `phonological-awareness` |
| 글자 만들기 | 기본 글자 만들기(자음·모음 합성) | `build-basic-letter` | `phonics` |
| 글자 만들기 | 받침 글자 만들기 | `build-batchim-letter` | `phonics` |
| 글자 만들기 | 겹받침 글자 만들기 | `build-double-batchim-letter` | `phonics` |
| 글자 자르기 | 받침 빼기(음소 생략형) | `remove-batchim` | `phonological-awareness` |
| 글자 자르기 | 음절 빼기(음절 생략형) | `remove-syllable` | `phonological-awareness` |
| 글자 대치 | 음절 바꾸기 | `replace-syllable` | `phonological-awareness` |

### 3.2 글 해독 및 문장 이해

| 세부 훈련 | 화면 표시명 | 기존 훈련 ID | 세션 카테고리 ID |
|---|---|---|---|
| 글 해독 | 낱말 읽기 | `read-real-words` | `fluency` |
| 글 해독 | 새 낱말 읽기(비단어) | `read-nonwords` | `fluency` |
| 글 해독 | 어려운 단어 먼저 읽기 | `hard-word` | `short-text` |
| 글 해독 | 문장 읽기 | `read-sentences` | `fluency` |
| 글 해독 | 짧은 글 읽기 | `read-short-passage` | `fluency` |
| 문장 완성 및 이해 | 문장 전체 조립 | `sentence-order` | `short-text` |
| 문장 완성 및 이해 | 빈칸에 알맞은 단어 넣기 | `fill-blank` | `short-text` |
| 문장 완성 및 이해 | 그림과 문장 연결하기 | `match-picture` | `short-text` |

### 3.3 유창성

| 세부 훈련 | 화면 표시명 | 기존 훈련 ID | 세션 카테고리 ID |
|---|---|---|---|
| 유창하게 읽기 | 문장 따라 읽기 | `follow-sentence` | `fluency` |
| 유창하게 읽기 | 단어 이어 읽기 | `word-chain` | `fluency` |
| 유창하게 읽기 | 끊어 읽기 | `phrase-reading` | `fluency` |
| 유창하게 읽기 | 같은 문장 다시 읽기 | `re-read` | `fluency` |
| 유창하게 읽기 | 짧은 이야기 읽기 | `short-story` | `fluency` |

## 4. 세션 진입 흐름 변경

훈련 선택과 실제 세션 실행에서 서로 다른 종류의 카테고리 ID를 사용한다.

```text
훈련 선택 홈
→ 표시 대분류 선택
→ 표시 카테고리 ID로 세부 훈련 모달 열기
→ 훈련 선택
→ 해당 훈련의 기존 카테고리 ID와 훈련 ID로 세션 진입
→ 기존 세션 로직으로 문제 진행·저장·완료
```

예를 들어 학습자가 `음운 인식 및 파닉스 > 글자 따라 보기 > 모음`을 선택하면 다음과 같이 처리한다.

```text
/learner/training/sound-phonics
→ 모음 선택
→ /learner/training/phonics/trace-vowel
→ startLesson(trace-vowel 원본 훈련 데이터)
```

`sound-phonics`는 메뉴 표시용 ID이고, 세션에는 기존 값인 `phonics`와 `trace-vowel`이 전달된다.

## 5. 세션에서 유지되는 항목

다음 항목은 이번 작업에서 변경하지 않았다.

### 5.1 세션 시작

- `getLessonById()`로 기존 원본 훈련 데이터를 조회한다.
- `startLesson()`에는 화면 표시용 복사본이 아닌 기존 원본 훈련 데이터를 전달한다.
- 세션 상태의 `categoryId`와 `lessonId`에는 기존 ID가 저장된다.

### 5.2 문제 진행

- 훈련별 액티비티 유형
- 문제 및 선택지 데이터
- 현재 문제 번호와 완료 문항 관리
- 정답 및 재시도 처리
- 힌트 처리
- 다음 문제 이동

### 5.3 결과 저장과 완료

- `saveResult()`를 이용한 결과 저장 과정
- 저장 성공 후 `completeLesson()`을 호출하는 과정
- 완료 시각과 완료 상태 관리
- 완료 화면 직접 진입 방지
- 완료 세션의 기존 카테고리 ID·훈련 ID 검증

### 5.4 평가와 녹음

- 평가 세션
- 상세 훈련 세션 상태 머신
- 시도 횟수와 오류 판정
- 음성 녹음 및 마이크 권한 처리
- 읽기 평가
- 기술 오류 제외 기준
- 중단 및 이어하기
- 백엔드 API와 결과 데이터 형식

## 6. 화면 표시명 처리

훈련의 기존 제목은 데이터 호환을 위해 변경하지 않았다. 대신 표시 전용 카탈로그에서 문서 기준 제목을 제공한다.

표시 전용 제목은 다음 화면에 적용된다.

1. 훈련 선택 모달
2. 훈련 인트로
3. 훈련 진행 화면 상단
4. 훈련 완료 화면

화면 표시명만 바뀌며, 세션 시작과 결과 저장에는 기존 훈련 ID와 원본 훈련 데이터가 사용된다.

## 7. 노출에서 제외한 훈련

### 7.1 한글 대결

다음 훈련은 화면 카탈로그와 직접 진입 가능한 훈련 목록에서 제외했다.

- `battle-rabbit`
- `battle-turtle`
- `battle-ant`

기존 원본 데이터와 액티비티 컴포넌트는 삭제하지 않았다. 다른 작업에 미치는 영향을 줄이기 위해 학습자 메뉴 노출과 라우트 진입만 차단했다.

### 7.2 중복 항목

다음 항목은 동일하거나 유사한 훈련으로 통합해 별도 메뉴로 표시하지 않는다.

| 제외한 훈련 ID | 통합해 표시하는 훈련 |
|---|---|
| `word-first-sound-choice` | `first-sound` |
| `batchim-sound` | `last-sound` |
| `repeat-sentence` | `follow-sentence` |

제외된 훈련도 원본 데이터에서는 삭제하지 않았으며, 학습자 메뉴와 직접 URL 진입만 제한한다.

## 8. 라우트 검증 변경

훈련 라우트는 두 단계로 검증한다.

### 8.1 대분류 모달 라우트

`/learner/training/:categoryId`에서는 표시 카테고리 ID만 허용한다.

- `sound-phonics`
- `decoding-comprehension`
- `fluency`

### 8.2 훈련 및 완료 라우트

훈련 실행 및 완료 라우트에서는 다음 조건을 모두 확인한다.

1. 기존 세션 카테고리 ID가 유효한가
2. 해당 카테고리에 기존 훈련 ID가 존재하는가
3. 실제 실행 가능한 훈련 데이터가 있는가
4. 현재 학습자 화면에 노출하도록 승인된 훈련인가

조건을 만족하지 않으면 훈련 선택 홈으로 이동한다. 따라서 한글 대결이나 중복으로 제외된 훈련은 URL을 직접 입력해도 진입할 수 없다.

## 9. 변경 파일

| 파일 | 변경 내용 |
|---|---|
| `apps/learner/src/mocks/trainingDisplayCatalog.ts` | 3개 대분류, 세부 훈련, 28개 표시 훈련 및 기존 ID 연결 정의 |
| `apps/learner/src/views/learner/TrainingHomeView.vue` | 표시 카테고리 기반 훈련 선택과 기존 세션 ID 기반 이동 처리 |
| `apps/learner/src/components/training/TrainingCategoryCard.vue` | 표시 카테고리 타입 적용 |
| `apps/learner/src/components/training/TrainingLessonModal.vue` | 세부 훈련별 그룹 목록 표시 |
| `apps/learner/src/app/router/index.ts` | 표시 카테고리 및 학습자 노출 훈련 검증 |
| `apps/learner/src/views/learner/TrainingLessonView.vue` | 화면 표시명 적용, 기존 원본 데이터로 세션 시작 |
| `apps/learner/src/views/learner/TrainingCompleteView.vue` | 완료 화면에 문서 기준 표시명 적용 |

## 10. 수정하지 않은 주요 파일

평가와 기록 작업을 보호하기 위해 다음 파일 및 영역은 수정하지 않았다.

| 파일 또는 영역 | 역할 |
|---|---|
| `apps/learner/src/composables/useTrainingSession.ts` | 기존 훈련 진행 및 결과 저장 세션 |
| `apps/learner/src/composables/useVoiceRecorder.ts` | 음성 녹음과 마이크 상태 |
| `apps/learner/src/features/assessment/**` | 평가 진행 및 결과 관리 |
| `apps/learner/src/features/training/composables/useDetailedTrainingSession.ts` | 상세 훈련 세션 상태 머신 |
| `apps/learner/src/features/training/engine/**` | 평가, 오류, 도움 및 완료 판정 |
| `apps/learner/src/features/training/repository/**` | 중단, 저장 및 이어하기 |
| 백엔드·API 영역 | 서버 저장과 데이터 계약 |

## 11. 검증 결과

- 화면 대분류: 3개
- 화면 표시 훈련: 28개
- 훈련 ID 중복: 없음
- 한글 대결 노출: 없음
- 중복 통합 항목 노출: 없음
- TypeScript 및 Vue 타입 검사: 통과
- Vite 프로덕션 빌드: 통과

## 12. 후속 개발 시 주의사항

1. 화면 분류를 변경할 때 기존 `TrainingCategoryId`를 바로 교체하지 않는다.
2. 서버 또는 평가 결과와 연결되는 훈련 ID는 별도 마이그레이션 없이 변경하지 않는다.
3. 새로운 훈련을 노출하려면 원본 훈련 데이터와 표시 카탈로그를 모두 등록한다.
4. 표시 카탈로그에는 동일한 훈련 ID를 두 번 등록하지 않는다.
5. 훈련 제목만 변경하는 경우 원본 세션 데이터보다 표시 카탈로그의 제목을 수정한다.
6. 숨겨진 훈련을 다시 노출할 때는 라우트 검증과 평가·기록 영향 범위를 함께 확인한다.
